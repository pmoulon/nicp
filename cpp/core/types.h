#pragma once

#include <utility>
#include <vector>

namespace ochre {
namespace core {

typedef std::pair<size_t, size_t> Association;
typedef std::vector<Association> Associations;

}  // namespace core
}  // namespace ochre
